server:
  port: 3355
spring:
  application:
    name: config-client
  cloud:
    config:
      # config客户端配置
      label: main
      name: application
      profile: dev
      # 读取main分支上的application-dev.yml文件
      uri: http://localhost:3344
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
